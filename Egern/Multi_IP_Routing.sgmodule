#!name=IP查询多路分流
#!desc=自定义分流：IPPure / IP-API / IPAPI.co
#!category=Custom
#!icon=arrow.branch

# === 关键修改 ===
# 格式: 显示名称:默认值
#!arguments=IPPure:Proxy, IP-API:Proxy, IPAPI:Proxy

# 描述文案也对应更新
#!arguments-desc=IPPure: 设置 ippure.com 的策略, IP-API: 设置 ip-api.com 的策略, IPAPI: 设置 ipapi.co 的策略

[Rule]
# 引用 IPPure
DOMAIN-SUFFIX,ippure.com,{{IPPure}}

# 引用 IP-API (注意：变量名必须和上面定义的完全一致)
DOMAIN-SUFFIX,ip-api.com,{{IP-API}}

# 引用 IPAPI
DOMAIN-SUFFIX,ipapi.co,{{IPAPI}}
